# Ground-Truth Data

Keep large images and masks out of normal Git commits. Track only this README, the manifest template, split files, and small de-identified annotations. Use DVC, Git LFS, or a citable archive for the binary files.

See `docs/ground_truth_data.md` for the full implementation plan.
